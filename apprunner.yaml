version: 1.0
runtime: python3 
build:
  commands:
    build:        
      - python3 -m venv env
      - source env/bin/activate
      - pip install -r requirements.txt
      - sudo ufw allow 8080
run:
  command: gunicorn --bind 0.0.0.0:8080 wsgi:app
  network:
    port: 8080